<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Course Details</title>
</head>

<body>
    <h1>Manage Course</h1>
    <a href="/instructor/dashboard">Back to Dashboard</a>

    <hr>

    <!-- TODO: Add Student Form -->
    <!-- Input: Student Username -->
    <!-- Action: /instructor/add-student -->
    <h3>Add Student</h3>

    <!-- TODO: List Enrolled Students -->
    <!-- Show: Student ID, Name -->
    <!-- Action: Remove Button -->
    <h3>Enrolled Students</h3>
    <ul>
        <% student_info.forEach(course =>{ %>
            <li><%= student_info.student_id %>, <%= student_info.username %>, <%= student_info.full_name %>
                <button onclick="removeStudent('<%= student_info.student_id %>')">Manage</button>
            </li>
        <%})%>
    </ul>

</body>

<script>

    async function removeStudent(studentID){
        try {
            const res = await fetch('/instructor/course/:id', {
                method:'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({course_id: courseId})
            });
            
            if(!res.ok){
                throw new Error('Request Failed');
            }
            
            location.reload();
        } catch (err){
            console.error(err);
            alert('Failed to remove student');
        }

    }

</script>

</html>